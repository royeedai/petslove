# 萌友益站 - 开发环境搭建指南

## 环境准备

### 必需软件

| 软件 | 版本要求 | 用途 | 下载地址 |
|-----|---------|------|---------|
| JDK | 17+ | Java开发环境 | https://www.oracle.com/java/technologies/downloads/ |
| Maven | 3.6+ | Java项目构建工具 | https://maven.apache.org/download.cgi |
| MySQL | 8.0+ | 关系型数据库 | https://dev.mysql.com/downloads/mysql/ |
| Redis | 5.0+ | 缓存数据库 | https://redis.io/download/ |
| Node.js | 16+ | 前端开发环境 | https://nodejs.org/ |
| Git | 最新版 | 版本控制 | https://git-scm.com/ |

### 开发工具

| 工具 | 推荐版本 | 用途 |
|-----|---------|------|
| IntelliJ IDEA | 2023.x+ | Java开发IDE |
| VS Code | 最新版 | 前端开发编辑器 |
| HBuilderX | 最新版 | UniApp开发工具 |
| Navicat / DBeaver | 最新版 | 数据库管理工具 |
| Another Redis Desktop Manager | 最新版 | Redis管理工具 |
| Postman / Apifox | 最新版 | API接口测试工具 |

---

## 一、后端环境搭建

### 1.1 安装JDK 17

#### Windows系统
1. 下载JDK 17安装包
2. 双击安装，记住安装路径（如：`C:\Program Files\Java\jdk-17`）
3. 配置环境变量：
   - `JAVA_HOME`: JDK安装路径
   - `Path`: 添加 `%JAVA_HOME%\bin`
4. 验证安装：
```bash
java -version
javac -version
```

#### macOS系统
```bash
# 使用Homebrew安装
brew install openjdk@17

# 配置环境变量（添加到 ~/.zshrc 或 ~/.bash_profile）
export JAVA_HOME=/usr/local/opt/openjdk@17
export PATH="$JAVA_HOME/bin:$PATH"

# 验证安装
java -version
```

#### Linux系统
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install openjdk-17-jdk

# CentOS/RHEL
sudo yum install java-17-openjdk-devel

# 验证安装
java -version
```

### 1.2 安装Maven

#### Windows系统
1. 下载Maven压缩包并解压到指定目录（如：`C:\Program Files\Maven`）
2. 配置环境变量：
   - `MAVEN_HOME`: Maven安装路径
   - `Path`: 添加 `%MAVEN_HOME%\bin`
3. 配置镜像源（可选，加速下载）：
   编辑 `conf/settings.xml`，在 `<mirrors>` 标签内添加：
```xml
<mirror>
  <id>aliyun</id>
  <mirrorOf>central</mirrorOf>
  <name>Aliyun Maven</name>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```
4. 验证安装：
```bash
mvn -version
```

#### macOS/Linux系统
```bash
# macOS
brew install maven

# Ubuntu/Debian
sudo apt install maven

# CentOS/RHEL
sudo yum install maven

# 验证安装
mvn -version
```

### 1.3 安装MySQL 8.0

#### Windows系统
1. 下载MySQL安装包
2. 运行安装程序，选择"Developer Default"
3. 设置root密码（建议：root）
4. 完成安装后，启动MySQL服务

#### macOS系统
```bash
# 使用Homebrew安装
brew install mysql@8.0

# 启动MySQL服务
brew services start mysql@8.0

# 初始化（首次安装）
mysql_secure_installation
```

#### Linux系统
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install mysql-server-8.0

# CentOS/RHEL
sudo yum install mysql-server

# 启动MySQL服务
sudo systemctl start mysqld
sudo systemctl enable mysqld

# 初始化
sudo mysql_secure_installation
```

#### 创建数据库
```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE mengyo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出
exit;

# 导入初始化脚本
mysql -u root -p mengyo_db < database/init.sql
```

### 1.4 安装Redis

#### Windows系统
1. 下载Redis for Windows: https://github.com/microsoftarchive/redis/releases
2. 解压到指定目录
3. 运行 `redis-server.exe` 启动服务

#### macOS系统
```bash
# 使用Homebrew安装
brew install redis

# 启动Redis服务
brew services start redis

# 测试连接
redis-cli ping
```

#### Linux系统
```bash
# Ubuntu/Debian
sudo apt install redis-server

# CentOS/RHEL
sudo yum install redis

# 启动Redis服务
sudo systemctl start redis
sudo systemctl enable redis

# 测试连接
redis-cli ping
```

### 1.5 导入后端项目

#### 使用IntelliJ IDEA
1. 打开IDEA，选择 `Open` 或 `Import Project`
2. 选择项目根目录下的 `mengyo-backend` 文件夹
3. 等待Maven依赖下载完成
4. 修改 `application-dev.yml` 配置：
   - 数据库连接信息
   - Redis连接信息
5. 找到 `MengyoApplication.java`，右键运行
6. 访问 http://localhost:8080/api/doc.html 查看API文档

#### 使用命令行
```bash
cd mengyo-backend

# 编译项目
mvn clean compile

# 运行项目
mvn spring-boot:run

# 或打包后运行
mvn clean package
java -jar target/mengyo-backend-1.0.0.jar
```

---

## 二、前端环境搭建

### 2.1 安装Node.js

#### Windows系统
1. 下载Node.js安装包（LTS版本）
2. 双击安装，保持默认设置
3. 验证安装：
```bash
node -v
npm -v
```

#### macOS/Linux系统
```bash
# macOS
brew install node@16

# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node -v
npm -v
```

### 2.2 配置npm镜像源（可选）

```bash
# 设置淘宝镜像源（加速下载）
npm config set registry https://registry.npmmirror.com

# 验证
npm config get registry

# 或使用nrm管理镜像源
npm install -g nrm
nrm use taobao
```

### 2.3 安装HBuilderX（UniApp开发）

1. 下载HBuilderX: https://www.dcloud.io/hbuilderx.html
2. 解压并运行
3. 安装插件：
   - uni-app编译器
   - 微信开发者工具支持
4. 配置微信开发者工具路径

### 2.4 小程序项目搭建（待创建）

```bash
# 创建UniApp项目
vue create -p dcloudio/uni-preset-vue mengyo-miniapp

# 进入项目目录
cd mengyo-miniapp

# 安装依赖
npm install

# 运行到微信小程序
npm run dev:mp-weixin

# 运行到H5
npm run dev:h5
```

### 2.5 管理后台项目搭建（待创建）

```bash
# 创建Vue3项目
npm create vite@latest mengyo-admin -- --template vue

# 进入项目目录
cd mengyo-admin

# 安装依赖
npm install

# 安装Element Plus
npm install element-plus

# 安装Vue Router
npm install vue-router@4

# 安装Pinia
npm install pinia

# 安装Axios
npm install axios

# 启动开发服务器
npm run dev
```

---

## 三、开发工具配置

### 3.1 IntelliJ IDEA配置

#### 安装插件
- Lombok
- MyBatis Plus
- Alibaba Java Coding Guidelines
- RestfulTool
- GitToolBox

#### 代码格式化
1. 导入阿里巴巴代码规范：
   - File -> Settings -> Editor -> Code Style
   - Import Scheme -> Alibaba Java Coding Guidelines

#### Maven配置
- File -> Settings -> Build, Execution, Deployment -> Build Tools -> Maven
- Maven home directory: 指定Maven安装路径
- User settings file: 指定settings.xml路径

### 3.2 VS Code配置

#### 安装插件
- Vue Language Features (Volar)
- TypeScript Vue Plugin (Volar)
- ESLint
- Prettier
- Vue VSCode Snippets
- Auto Rename Tag
- GitLens

#### 配置文件（settings.json）
```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  }
}
```

---

## 四、常见问题解决

### 4.1 Maven依赖下载慢
**解决方案**: 配置阿里云镜像源（见1.2节）

### 4.2 MySQL连接失败
**可能原因及解决方案**:
1. MySQL服务未启动 -> 启动MySQL服务
2. 端口被占用 -> 修改MySQL端口
3. 密码错误 -> 重置MySQL密码
4. 防火墙阻止 -> 关闭防火墙或开放3306端口

### 4.3 Redis连接失败
**可能原因及解决方案**:
1. Redis服务未启动 -> 启动Redis服务
2. 端口被占用 -> 修改Redis端口
3. 绑定地址问题 -> 修改redis.conf中的bind配置

### 4.4 端口被占用
```bash
# Windows查看端口占用
netstat -ano | findstr "8080"
taskkill /PID <进程ID> /F

# macOS/Linux查看端口占用
lsof -i:8080
kill -9 <进程ID>
```

### 4.5 npm安装依赖失败
```bash
# 清除npm缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

---

## 五、验证环境

### 5.1 后端验证

```bash
# 启动后端项目
cd mengyo-backend
mvn spring-boot:run

# 测试接口（新开终端）
curl http://localhost:8080/api/doc.html
```

✅ 成功标志：
- 控制台输出 "萌友益站后端服务启动成功！"
- 可以访问Swagger文档页面
- 数据库连接正常
- Redis连接正常

### 5.2 前端验证

```bash
# 启动小程序（待创建）
cd mengyo-miniapp
npm run dev:mp-weixin

# 启动管理后台（待创建）
cd mengyo-admin
npm run dev
```

✅ 成功标志：
- 编译成功无错误
- 可以在浏览器访问页面
- 可以调用后端接口

---

## 六、推荐学习资源

### 后端
- Spring Boot官方文档: https://spring.io/projects/spring-boot
- MyBatis-Plus文档: https://baomidou.com/
- Redis中文文档: http://redis.cn/

### 前端
- Vue 3文档: https://cn.vuejs.org/
- UniApp文档: https://uniapp.dcloud.net.cn/
- Element Plus文档: https://element-plus.org/zh-CN/

---

## 七、团队协作

### Git工作流程

```bash
# 1. 克隆仓库
git clone <repository-url>

# 2. 创建功能分支
git checkout -b feature/your-feature-name

# 3. 开发并提交
git add .
git commit -m "[feat] your feature description"

# 4. 推送到远程
git push origin feature/your-feature-name

# 5. 创建Pull Request
# 在GitHub/GitLab上创建PR，等待代码审查
```

### 提交信息规范
```
[feat] 新功能
[fix] 修复bug
[docs] 文档更新
[style] 代码格式调整
[refactor] 代码重构
[test] 测试相关
[chore] 构建/工具变动
```

---

## 八、下一步

环境搭建完成后，请查看：
1. [项目架构设计文档](项目架构设计文档.md) - 了解整体架构
2. [开发进度跟踪](开发进度跟踪.md) - 查看当前进度
3. [第一阶段开发计划](第一阶段-基础框架开发计划.md) - 开始开发

---

**祝您开发顺利！有任何问题请随时联系团队。** 🚀
